<h3>Verify code</h3>
<p>Because your account has two-factor authentication enabled, you now need to enter the code shown in your mobile
  authenticator app in the field below.</p>

<%= form_tag url_for(controller: :custom_sessions, action: :verify_code) do %>
  <%= hidden_field_tag 'uid', params[:uid] %>

  <div class="field">
    <%= label_tag 'code' %><br/>
    <%= text_field_tag 'code', '', class: 'form-control' %>
  </div><br/>

  <div class="actions">
    <%= submit_tag 'Verify', class: 'btn btn-success' %>
  </div>
<% end %>