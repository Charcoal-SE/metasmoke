<%= form_for(flag_setting) do |f| %>
  <% if flag_setting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(flag_setting.errors.count, "error") %> prohibited this flag_setting from being saved:</h2>

      <ul>
      <% flag_setting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :value %>
    <%= f.text_field :value %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
