<h3>User Feedback</h3>

<p>
  <strong><%= @feedback_count %></strong>
  feedbacks,
  <strong><%= @invalid_count %></strong>
  invalidated
  (<strong><%= ((@invalid_count.to_f / @feedback_count.to_f) * 100).round(2) %>%</strong> invalid)
</p>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Post</th>
      <th>Feedback Type</th>
      <th>Invalidated</th>
    </tr>
  </thead>
  <tbody>
    <% @feedbacks.each do |f| %>
      <tr>
        <td><a href="/post/<%= f.post_id %>"><%= f.title %></a></td>
        <td><%= f.feedback_type %></td>
        <td><%= f.invalidated_at.nil? ? "No" : "Yes" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<div class="text-center">
  <%= will_paginate @feedbacks, renderer: BootstrapPagination::Rails %>
</div>
