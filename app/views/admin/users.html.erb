<h3>Users</h3>
<p>A list of all user accounts on the site, with useful links about them and their content. No PII, though - if you
  <em>really</em> need access to that, ping Undo.</p>

<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th>Username</th>
      <th>Review-approved?</th>
      <th>Admin?</th>
      <th>Code Admin?</th>
      <th colspan="2"></th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.id %></td>
        <td><%= user.username %></td>
        <td><%= user.is_approved ? "Yes" : "No" %></td>
        <td><%= user.is_admin ? "Yes" : "No" %></td>
        <td><%= user.is_code_admin ? "Yes" : "No" %></td>
        <td><%= link_to "Feedback", admin_user_feedback_path(:user_id => user.id) %></td>
        <% if user_signed_in? && current_user.is_admin %>
          <td>
            <% unless user.is_code_admin %>
              <%= link_to "Grant code access", url_for(:controller => :admin, :action => :promote_code_admin, :id => user.id), :method => :post, :class => "text-danger" %>
            <% else %>
              <%= link_to "Revoke code access", url_for(:controller => :admin, :action => :demote_code_admin, :id => user.id), :method => :post, :class => "text-danger" %>
            <% end %>
          </td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
