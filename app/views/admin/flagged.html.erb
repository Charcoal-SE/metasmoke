<h3>Reported Posts</h3>
<p>A list of posts that have been flagged for admin attention by users. They should have reasons with them that the
  user submitted when reporting; do the needful then dismiss it.</p>

<table class="table">
  <% @flags.each do |flag| %>
    <% user = @users.find flag.user_id if flag.user_id %>
    <div>
      <%= render 'posts/post', :post => flag.post %>
      <tr>
        <td>
          <p>
            <strong>Reason:</strong> <%= flag.reason %>
            <% if user %>
              &mdash; <strong><%= user.username unless user.username.nil? %></strong>
            <% end %>
          </p>
          <a href="#" class="admin-report-done btn btn-sm btn-success" data-post-id="<%= flag.post.id %>">Dismiss</a>
        </td>
      </tr>
    </div>
  <% end %>
</table>
