<p id="notice"><%= notice %></p>

<h3>Recent posts</h3>

<table class="table">
  <thead>
    <tr>
      <th>Post</th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <%= render "post", :post => post %>
    <% end %>
  </tbody>
</table>

<%= will_paginate @posts %>
