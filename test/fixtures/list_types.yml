notifications:
  name: notifications
  description: User pings for specific message types in specific rooms.

blacklist:
  name: blacklist
  description: Blacklist of some sort, requiring permissions for testing.
  permissions: code_admin